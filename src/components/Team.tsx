import * as React from "react";

interface IAppProps {}

const Team: React.FunctionComponent<IAppProps> = (props) => {
  return (
    <div>
      <h1>Team</h1>
    </div>
  );
};

export default Team;
