import * as React from "react";

interface IAppProps {}

const CreateActivities: React.FunctionComponent<IAppProps> = (props) => {
  return (
    <div>
      crear por nombre, asignar material(nombre de material, cantidad y
      unidades), asignar prioridad...... agregar al array de project
    </div>
  );
};

export default CreateActivities;
