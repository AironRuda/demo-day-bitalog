import * as React from "react";

interface IAppProps {}

const Inventory: React.FunctionComponent<IAppProps> = (props) => {
  return (
    <div>
      <h1>Inventory</h1>
    </div>
  );
};

export default Inventory;
